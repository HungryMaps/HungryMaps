<center>
<br>
<h1 style="color:green;" >Iniciar Sesión</h1>

<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  <div><%= f.label :email, "E-mail" %><br />
  <%= f.email_field :email, autofocus: true %></div>

  <div><%= f.label :password, "Contraseña"%><br />
    <%= f.password_field :password, autocomplete: "off" %></div>

  <% if devise_mapping.rememberable? -%>
    <div><%= f.check_box :remember_me %> <%= f.label :remember_me, "Recordar mi cuenta" %></div>
  <% end -%>

  <div><%= f.submit "Iniciar Sesión" %></div>
<% end %>

<%= render "devise/shared/links" %>

</center>
