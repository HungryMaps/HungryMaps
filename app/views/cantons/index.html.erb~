<h1>Listando cantones</h1>

<table>
  <thead>
    <tr>
      <th>Nombre canton</th>
      <th>Provincia</th>
      <th>Direccion</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @cantons.each do |canton| %>
      <tr>
        <td><%= canton.nombre_canton %></td>
        <td><%= canton.provin_id %></td>
        <td><%= canton.direccion_id %></td>
        <td><%= link_to canton, canton %></td>
        <td><%= link_to 'Editar', edit_canton_path(canton) %></td>
        <td><%= link_to 'Destruir', canton, method: :delete, data: { confirm: 'Está Seguro?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nuevo Canton', new_canton_path %>
