<h1>Listado categorias</h1>

<table>
  <thead>
    <tr>
      <th>Nombre categoria</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @categors.each do |categor| %>
      <tr>
        <td><%= categor.nombre_categoria %></td>
        <td><%= link_to 'Mostrar', categor %></td>
        <td><%= link_to 'Editar', edit_categor_path(categor) %></td>
        <td><%= link_to 'Destruir', categor, method: :delete, data: { confirm: '¿Está Seguro?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nueva Categoria', new_categor_path %>
