<%= form_for(@categor) do |f| %>
  <% if @categor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@categor.errors.count, "error") %> prohibited this categor from being saved:</h2>

      <ul>
      <% @categor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre_categoria %><br>
    <%= f.text_field :nombre_categoria %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
